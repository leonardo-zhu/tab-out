node_modules/
*.log
*.db
.DS_Store
docs/superpowers/
build/
*.tsbuildinfo
.idea

# Personal config (landing page patterns, etc.) — never push to GitHub
extension/config.local.js

# Build outputs (Vite generates these)
extension/assets/
extension/index.html
extension/.version
